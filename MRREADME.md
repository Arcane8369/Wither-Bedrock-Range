# Wither Bedrock Range

让凋灵（Wither）在**受伤时**对周围方块的破坏范围对齐**基岩版（Bedrock Edition）**。这是一个轻量、无配置、客户端与服务端通用的模组。

英文 / English:

> Makes the Wither's block-destruction range (triggered whenever it takes damage) match **Bedrock Edition**. A lightweight, configuration-free, client-and-server mod.

---

## 特性 / Features

中文 / Chinese:

- **Java 版原版行为**：凋灵受伤时只破坏约 **3 × 4 × 3** 的方块区域
- **本模组**：改为破坏更符合基岩版的 **4 × 6 × 4**（X × Y × Z）方块区域
- 仅用一次 Mixin 微调，不动其他任何游戏逻辑；无需配置文件，放进 `mods/` 即生效
- 仅改变破坏范围的大小，不影响凋灵的血量、AI、掉落或其它行为

英文 / English:

1. **Vanilla Java:** the Wither destroys only a ~3 × 4 × 3 block area when damaged.
2. **This mod:** it destroys a **4 × 6 × 4** (X × Y × Z) box, matching Bedrock Edition.
3. Made with a single lightweight Mixin; no other game logic is touched and no config file is needed—just drop it into `mods/`.
4. Only the destruction size changes; the Wither's health, AI, drops and all other behaviour stay untouched.

---

## 支持版本 / Supported versions

中文 / Chinese:

- **NeoForge 1.21.1**：主版本（本仓库默认）。
- **Forge 1.20 / 1.20.1**：移植版，位于 `port/1.20.1-forge` 分支。

英文 / English:

1. **NeoForge 1.21.1:** the main version (default in this repository).
2. **Forge 1.20 / 1.20.1:** the port, on the `port/1.20.1-forge` branch.

---

## 安装 / Installation

中文 / Chinese:

1. 把 JAR 放入客户端 `.minecraft/mods/`（和/或专用服务器的 `mods/`）。
2. 启动游戏，召唤凋灵并攻击它即可看到基岩版风格的破坏范围。

英文 / English:

1. Drop the JAR into your client `.minecraft/mods/` (and/or the dedicated server's `mods/`).
2. Launch the game, then summon and hit a Wither to see the Bedrock-style destruction.

---

## 兼容性 / Compatibility

中文 / Chinese:

1. 客户端与服务端**均需安装**。
2. 基于官方映射/原始映射改写，与大多数模组兼容。
3. 已知与同为「凋灵行为大改」类模组可能存在逻辑重叠，本模组未充分进行测试，若遇冲突请二选一。
4. 可独立工作于整合包中，无需其它前置模组依赖。

英文 / English:

1. Must be installed on **both client and server**.
2. Built against official / original mappings, so it is compatible with most mods.
3. May overlap with other Wither-overhaul mods; this mod has not been fully tested alongside them, so if a conflict occurs, keep only one.
4. Works standalone in a modpack and requires no other dependencies.

---

## 开源 / Open Source

中文 / Chinese:

1. 源码仓库：**[github.com/Sean8369/Wither-Bedrock-Range](https://github.com/Sean8369/Wither-Bedrock-Range)**。
2. 基于 [GNU General Public License v3.0](https://github.com/Sean8369/Wither-Bedrock-Range/blob/main/LICENSE.txt) 发布。
3. 构建方式与源码改动详见仓库 [README.md](https://github.com/Sean8369/Wither-Bedrock-Range/blob/main/README.md)。

英文 / English:

1. Source repository: **[github.com/Sean8369/Wither-Bedrock-Range](https://github.com/Sean8369/Wither-Bedrock-Range)**.
2. Released under the [GNU General Public License v3.0](https://github.com/Sean8369/Wither-Bedrock-Range/blob/main/LICENSE.txt).
3. Build instructions and source changes are detailed in the repository [README.md](https://github.com/Sean8369/Wither-Bedrock-Range/blob/main/README.md).

---

## 署名与 AI 编写声明 / Attribution & AI-generated disclaimer

**作者 / Authors：** Arcane

中文 / Chinese:

> 本项目由 AI（DeepSeek-V4）自动生成，按 "AS IS" 交付，**不对代码质量、正确性、稳定性、安全性或与任何第三方软件的兼容性做任何保证**。请在使用前自行审查代码、充分测试，并自行承担使用本项目可能产生的全部风险。

英文 / English:

> This project is auto-generated by AI (DeepSeek-V4), provided "AS IS", with **no warranty** as to quality, correctness, reliability, security, or compatibility. Review the code and test it thoroughly before use, and assume full responsibility for any risks arising from its use.